# Requirements for finetune_swin_emoji notebooks (V5, V6, V7)
# Install with: pip install -r requirements.txt --upgrade

# Core deep learning frameworks
torch>=2.6.0
torchvision>=0.15.0
torchaudio>=2.6.0   # ADDED THIS to fix the dependency conflict
transformers>=4.30.0
accelerate>=0.20.0

# Data handling and processing
numpy>=1.24.0
pandas>=2.0.0
pillow>=9.5.0
datasets>=2.12.0

# Machine learning utilities
scikit-learn>=1.3.0
xgboost>=2.0.0  # Required for V6 only
lightgbm        # Added from bottom of your list

# Visualization (used for confusion matrices)
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Dataset download
kagglehub>=0.2.0

# Note: Standard library modules (os, pathlib, json, random, hashlib, collections, traceback)
# are included with Python and do not need to be installed